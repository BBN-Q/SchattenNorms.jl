language: julia
julia:
  - 0.7
  - 1.0
  - nightly
matrix:
  allowed_failures:
    - julia: nightly
notifications:
  email: false
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("SchattenNorms"); Pkg.test("SchattenNorms"; coverage=true)'
